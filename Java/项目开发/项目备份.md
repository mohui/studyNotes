[
"Monday",
"Tuesday",
"Wednesday",
"Thursday",
"Friday",
"Saturday",
"Sunday"
]

log.debug("添加字典日志{}")

人群分类（HYPERTENSION-高血压,DIABETES-糖尿病,ACUTE_CORONARY_DISEASE-冠心病,CEREBRAL_STROKE-脑卒中,COPD-慢阻肺）

```markdown
@regexp(/MONDAY|TUESDAY|WEDNESDAY|THURSDAY|FRIDAY|SATURDAY|SUNDAY/)
```

```markdown
override fun list(): List<Inner> {
        val list = table
            .select()
            .where(MrMedicationRemindTable.KnPatientId.eq(arg(1)))
            .find()
        //region 测试代码

        // 获取当前时间是周几 // THURSDAY
        //        val week = LocalDateTime.now().dayOfWeek.name
        //        println("今天是周几${week}")
        //        val parse = DateUtil.parse("1994-10-11 12:11:12")
        //        val hour = parse.hour(true)
        //        println("哈哈小时")
        //        println("小时$hour")

        val remindList = list.filter {
            it.knStatus == 1
        }.map {
            val obj1 = mutableMapOf<String, Any?>();
            // 小时
            val hour = it.knTime.hour
            // 分钟
            val minute = it.knTime.minute;
            println("小时$hour")
            println("分钟$minute")

            // 星期数组
            val weeks = mutableListOf<String>()
            // 天（星期）（1~7 1=SUN 或 SUN，MON，TUE，WED，THU，FRI，SAT）
            if (it.knMonday == BooleanIntEnum.TRUE.value) weeks.add("MON")
            if (it.knTuesday == BooleanIntEnum.TRUE.value) weeks.add("TUE")
            if (it.knWednesday == BooleanIntEnum.TRUE.value) weeks.add("WED")
            if (it.knThursday == BooleanIntEnum.TRUE.value) weeks.add("THU")
            if (it.knFriday == BooleanIntEnum.TRUE.value) weeks.add("FRI")
            if (it.knSaturday == BooleanIntEnum.TRUE.value) weeks.add("SAT")
            if (it.knSunday == BooleanIntEnum.TRUE.value) weeks.add("SUN")

            // 零秒 零分 零时 日期  月份 星期
            // 00   00  00   ?    *   *
            obj1["id"] = it.knId
            obj1["patient"] = it.knPatientId
            obj1["knDrugName"] = it.knDrugName
            obj1["knTime"] = it.knTime
            // 零秒 零分 零时 日期  月份 星期
            obj1["corn"] = "00 $minute $hour ? * ${if (weeks.size == 7) "*" else weeks.joinToString(",")}}"
            obj1
        }
        println(remindList)

        // endregion

        return list.map {
            val weeks = mutableListOf<Week>()

            if (it.knMonday == BooleanIntEnum.TRUE.value) weeks.add(Week.MONDAY)
            if (it.knTuesday == BooleanIntEnum.TRUE.value) weeks.add(Week.TUESDAY)
            if (it.knWednesday == BooleanIntEnum.TRUE.value) weeks.add(Week.WEDNESDAY)
            if (it.knThursday == BooleanIntEnum.TRUE.value) weeks.add(Week.THURSDAY)
            if (it.knFriday == BooleanIntEnum.TRUE.value) weeks.add(Week.FRIDAY)
            if (it.knSaturday == BooleanIntEnum.TRUE.value) weeks.add(Week.SATURDAY)
            if (it.knSunday == BooleanIntEnum.TRUE.value) weeks.add(Week.SUNDAY)


            Inner(it.knId, it.knDrugName, it.knTime, it.knStatus == 1, weeks)
        }
    }
```

```markdown
                .where(MrMedicationRemindTable.KnTime gte arg(start))
                .where(MrMedicationRemindTable.KnTime lte arg(end))
```

```
    exclude("com.bjknrt.user-permission-centre","user-permission-centre-security")
```

```markdown

    implementation("com.xuxueli:xxl-job:2.3.1")

  config: classpath:logback.xml
#server:
#  port: 8081
xxl:
  job:
    accessToken: default_token
    admin:
      addresses: http://127.0.0.1:8080/xxl-job-admin
    executor:
      address: ''
      appname: xxl-job-executor-sample
      ip: ''
      logpath: ./data/applogs/xxl-job/jobhandler
      logretentiondays: 30
      port: 9999
```

```
return HtnVisit(
            hypertensionVisitList.patientId,
            hypertensionVisitList.id,
            hypertensionVisitList.patientName,
            hypertensionVisitList.doctorId,
            hypertensionVisitList.doctorName,
            hypertensionVisitList.followWay,
            hypertensionVisitList.followDate,
            hypertensionVisitList.isSymptomNone,
            hypertensionVisitList.isSymptomDizzinessHeadache,
            hypertensionVisitList.isSymptomNauseaVomiting,
            hypertensionVisitList.isSymptomBlurredTinnitus,
            hypertensionVisitList.isSymptomBreathing,
            hypertensionVisitList.isSymptomPalpitationsToc,
            hypertensionVisitList.isSymptomNoseBleed,
            hypertensionVisitList.isSymptomNumbness,
            hypertensionVisitList.isSymptomLowerExtremityEdema,
            hypertensionVisitList.symptomOther,
            hypertensionVisitList.signsSbp,
            hypertensionVisitList.signsDbp,
            hypertensionVisitList.signsHeight,
            hypertensionVisitList.signsWeight,
            hypertensionVisitList.signsHeartRate,
            hypertensionVisitList.signsBim,
            hypertensionVisitList.signsOther,
            hypertensionVisitList.lifeCigarettesPerDay,
            hypertensionVisitList.lifeAlcoholPerDay,
            hypertensionVisitList.lifeSportPerWeek,
            hypertensionVisitList.lifeSportPerTime,
            hypertensionVisitList.lifeSaltSituation,
            hypertensionVisitList.lifeMentalAdjustment,
            hypertensionVisitList.lifeFollowMedicalAdvice,
            hypertensionVisitList.abbreviationsAboutExamination,
            hypertensionVisitList.drugCompliance,
            hypertensionVisitList.isAdr,
            hypertensionVisitList.adrDesc,
            hypertensionVisitList.visitClass,
            hypertensionVisitList.isReferral,
            hypertensionVisitList.referralReason,
            hypertensionVisitList.referralAgencies,
            hypertensionVisitList.nextVisit,
            hypertensionVisitList.createdBy,
            hypertensionVisitList.createdAt,
            hypertensionVisitList.updatedBy,
            hypertensionVisitList.updatedAt,
            drugsResult
        )
        ```